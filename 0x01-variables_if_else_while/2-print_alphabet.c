#include <stdio.h>

/**
 * main - variables_if_else_while
 *
 * Description: 'print_alphabet'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
for (char i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
