#include "main.h"

/**
 * _abs - return the absolute value of a given integer.
 * @n: value use to find the absolute value.
 * Return the abs of any number
 */

int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
