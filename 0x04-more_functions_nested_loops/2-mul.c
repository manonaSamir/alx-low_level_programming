#include "main.h"

/**
 * mul -  function that multiplies two integers.
 * @a: the character to be multiple
 * @b: the character to be multiple
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}
